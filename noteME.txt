git clone ..
git remote ..
alias graph=git log --all --decorate --oneline --graph
git add .
git commit -am ""
git push
git branch (nama branch)
git checkout (nama branch
git merge (nama yang akan di merege)mengecek commit nya sudah sampe mana

git config --global --user.name "ramdankhan"
git config --global --user.email "ramdankhan"
git fetch (nama origin)
git branch -d (nama branch) menghapus branch
git checkout -b "nama branch" (untuk membuat branc + switch ke branch baru") 
